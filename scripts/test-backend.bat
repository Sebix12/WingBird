@echo off
set get=%*

echo Hello!
echo This script can be run in a source file
echo using "runscript". The difference between
echo script files and components are that
echo components are run when they are compiled.
echo However, script files are run when a user
echo visits the site (just like PHP)!